# AiSprint Platform - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLIENT SIDE (Next.js)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐    ┌──────────────────┐   ┌────────────────┐  │
│  │   Navbar     │    │  Auth Pages      │   │  Dashboard     │  │
│  │ - Sign In    │    │  - Signup        │   │  - 5 Tabs      │  │
│  │ - Sign Up    │    │  - Signin        │   │  - Profile     │  │
│  │ - Dashboard  │    │                  │   │  - Courses     │  │
│  │ - Signout    │    │                  │   │  - Wishlist    │  │
│  └──────────────┘    └──────────────────┘   │  - Certs       │  │
│                                              │  - Settings    │  │
│                                              └────────────────┘  │
│  ┌──────────────────────────┐  ┌──────────────────────────────┐  │
│  │   Course Pages           │  │   Payment Pages              │  │
│  │   - Course Details       │  │   - Payment Checkout         │  │
│  │   - Curriculum           │  │   - Order Summary            │  │
│  │   - Module List          │  │   - Success Confirmation     │  │
│  │   - Add to Wishlist      │  │   - Next Steps               │  │
│  └──────────────────────────┘  └──────────────────────────────┘  │
│                                                                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                 ┌─────────▼──────────┐
                 │  Global Auth        │
                 │  Context Provider   │
                 │  - useAuth()        │
                 └─────────┬──────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                      │
┌───────▼────────────────────────────────────▼──────────────────┐
│                  API Layer (Next.js Routes)                   │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌──────────────┐  ┌────────────────┐   │
│  │  Auth APIs       │  │  User APIs   │  │  Payment APIs  │   │
│  │  - /auth/signup  │  │  - /user/*   │  │  - /payment/*  │   │
│  │  - /auth/signin  │  │    courses   │  │    create-order│   │
│  │  - /auth/signout │  │    wishlist  │  │    verify      │   │
│  │  - /auth/me      │  │    profile   │  │                │   │
│  │                  │  │    password  │  │                │   │
│  └──────────────────┘  │    settings  │  └────────────────┘   │
│                         └──────────────┘                        │
└──────────────┬─────────────────────────┬──────────────────────┘
               │                         │
        ┌──────▼──────┐        ┌────────▼─────────┐
        │  Database   │        │  Razorpay API    │
        │  (PostgreSQL)       │  (Payment Gateway)│
        └──────┬──────┘        └────────────────┐ │
               │                                  │
     ┌─────────┴─────────────────┐               │
     │                           │               │
┌────▼─────┐  ┌────────┐  ┌──────▼───┐         │
│  users   │  │ courses│  │payments  │  ...     │
│  table   │  │ table  │  │ table    │         │
└──────────┘  └────────┘  └──────────┘         │
                                                │
                             ┌──────────────────┘
                             │
                        ┌────▼────────┐
                        │   Returns    │
                        │  JSON Data   │
                        └─────────────┘
```

---

## 🔄 User Flow Diagram

### Sign Up Flow
```
Start → Signup Page → Form Validation ✓
  ↓
Select Courses (1 or 2)
  ↓
Hash Password
  ↓
Create User Record
  ↓
Create Course Enrollments
  ↓
Set Auth Cookie
  ↓
Redirect to Dashboard
```

### Course Purchase Flow
```
View Course → Click "Enroll Now" → Payment Page
  ↓
Create Razorpay Order (Backend)
  ↓
Open Razorpay Checkout
  ↓
User Completes Payment
  ↓
Backend Verifies Signature
  ↓
Create Payment Record
  ↓
Add to user_courses
  ↓
Remove from Wishlist (if exists)
  ↓
Redirect to Success Page
  ↓
Auto-Redirect to Dashboard
```

### Dashboard Navigation
```
Authenticated User → Dashboard (5 Tabs)
  ├── General Tab (Profile Management)
  ├── Courses Tab (Enrolled Courses)
  ├── Wishlist Tab (Save for Later)
  ├── Certificates Tab (Achievements)
  └── Settings Tab (Account Security)
```

---

## 🗄️ Database Schema Diagram

```
┌─────────────────────────────┐
│         USERS               │
├─────────────────────────────┤
│ PK: id (UUID)               │
│ --- username (UNIQUE)       │
│ --- email (UNIQUE)          │
│ --- password_hash           │
│ --- name                    │
│ --- country                 │
│ --- bio                     │
│ --- is_active               │
│ --- created_at              │
│ --- updated_at              │
└────────┬────────────────────┘
         │
         │ 1:N
         │
    ┌────▼──────────────────────┐
    │   USER_COURSES            │
    ├───────────────────────────┤
    │ PK: id (UUID)             │
    │ FK: user_id → users.id    │
    │ --- course_type           │
    │ --- status                │
    │ --- experience            │
    │ --- career_goal           │
    │ --- enrollment_date       │
    │ --- created_at            │
    │ --- updated_at            │
    └───────────────────────────┘

         │
         │
    ┌────▼──────────────────────┐
    │   WISHLIST                │
    ├───────────────────────────┤
    │ PK: id (UUID)             │
    │ FK: user_id → users.id    │
    │ --- course_type           │
    │ --- added_at              │
    │ UNIQUE(user_id, course)   │
    └───────────────────────────┘

         │
         │
    ┌────▼──────────────────────┐
    │   CERTIFICATES            │
    ├───────────────────────────┤
    │ PK: id (UUID)             │
    │ FK: user_id → users.id    │
    │ --- course_type           │
    │ --- certificate_url       │
    │ --- issued_date           │
    │ --- created_at            │
    └───────────────────────────┘

         │
         │
    ┌────▼──────────────────────┐
    │   PAYMENTS                │
    ├───────────────────────────┤
    │ PK: id (UUID)             │
    │ FK: user_id → users.id    │
    │ --- course_type           │
    │ --- amount                │
    │ --- currency              │
    │ --- razorpay_order_id     │
    │ --- razorpay_payment_id   │
    │ --- status                │
    │ --- created_at            │
    │ --- updated_at            │
    └───────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌───────────────────────────────────────────────────────┐
│ Client Sends Sign In Request                         │
│ {email, password}                                     │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
         ┌──────────────────┐
         │ Find User by     │ ─┐
         │ Email from DB    │  │
         └──────────────────┘  │
                 │              │
                 ▼              │
         ┌──────────────────┐  │
         │ User Exists?     │  │
         └┬─────────────┬───┘  │
          │ NO          │ YES  │
          │             │      │
         ▼             ▼       │
        ❌            ┌─────────────────┐
      Invalid         │ Verify Password │
                      │ (PBKDF2)        │
                      └──┬──────────┬───┘
                         │         │
                       ❌│        YES│✓
                        ▼          │
                     Invalid       ▼
                           ┌────────────────────┐
                           │ Generate Token     │
                           │ Set HTTP Cookie    │
                           │ Return 200 OK      │
                           └────────────────────┘
                                   │
                                   ▼
                              Client Stores
                              in Cookie
```

---

## 💳 Payment Integration

```
FRONTEND                          BACKEND                    RAZORPAY
   │                                 │                          │
   │ Click "Pay"                     │                          │
   ├────────────────────────────────>│                          │
   │                                 │ POST /payment/create     │
   │                                 │ {course_type, amount}    │
   │                                 ├─────────────────────────>│
   │                                 │                          │
   │                                 │ Create Order Response    │
   │                                 │ {order_id, amount}       │
   │                                 │<─────────────────────────┤
   │                                 │                          │
   │ Load Razorpay Script            │                          │
   │ & Open Checkout                 │                          │
   │ {key_id, order_id}              │                          │
   ├────────────────────────────────────────────────────────────>│
   │                                                             │
   │ User Enters Card Details (Razorpay handles)                │
   │                                                             │
   │ Payment Processed <──────────────── Razorpay API           │
   │                                                             │
   │ Response {payment_id, order_id, signature}                 │
   │<────────────────────────────────────────────────────────────┤
   │                       │                                      │
   │ Send Payment Details  │                                      │
   ├────────────────────────────────────────>                    │
   │                       │ POST /payment/verify                │
   │                       │ {order_id, payment_id, signature}   │
   │                       │                                      │
   │                       │ Verify Signature (HMAC-SHA256)      │
   │                       │                                      │
   │                       ├─────────────────────────────────┐   │
   │                       │ If Valid:                       │   │
   │                       │ • Update Payment Status         │   │
   │                       │ • Create Course Enrollment      │   │
   │                       │ • Remove from Wishlist          │   │
   │                       │                                 │   │
   │                       │ Return {success: true}          │   │
   │<────────────────────────────────────────────────────────┤   │
   │                                                             │
   │ Redirect to Success Page                                    │
   │ Show Confirmation                                           │
   │ Auto-Redirect to Dashboard                                  │
```

---

## 📊 State Management

### Global Auth Context
```
┌─────────────────────────────────┐
│   AuthContext                   │
├─────────────────────────────────┤
│ State:                          │
│ • user (User | null)            │
│ • isAuthenticated (boolean)     │
│ • isLoading (boolean)           │
│                                  │
│ Methods:                        │
│ • signin(email, password)       │
│ • signup(...data, courses)      │
│ • signout()                     │
│ • refreshUser()                 │
└─────────────────────────────────┘
         │
         │ Provides to all children
         │
    ┌────▼──────────────────────┐
    │ useAuth() Hook Available   │
    │ to Components              │
    └────────────────────────────┘
```

---

## 🎯 Component Hierarchy

```
RootLayout
├── AuthProvider
│   ├── Navbar
│   │   ├── Logo
│   │   ├── NavLinks
│   │   └── AuthButtons (conditional)
│   │
│   ├── Main Content
│   │   ├── Dashboard (Auth)
│   │   │   ├── GeneralTab
│   │   │   ├── CoursesTab
│   │   │   ├── WishlistTab
│   │   │   ├── CertificatesTab
│   │   │   └── SettingsTab
│   │   │
│   │   ├── AuthPages
│   │   │   ├── SignupPage
│   │   │   │   └── RegistrationForm
│   │   │   │
│   │   │   └── SigninPage
│   │   │       └── SignInForm
│   │   │
│   │   └── CoursePages
│   │       ├── CourseDetailPage
│   │       ├── PaymentPage
│   │       └── SuccessPage
│   │
│   ├── Footer
│   └── ChatBox
```

---

## 🚀 Data Flow Example (Course Purchase)

```
1. User at Dashboard
   ↓
2. Clicks "Enroll Now" on Course
   ↓
3. Frontend Route: /dashboard/course/{courseType}/payment
   ↓
4. Display Payment Page with Order Summary
   ↓
5. User Clicks "Pay {price}"
   ↓
6. Frontend calls POST /api/payment/create-order
   ├── Body: {course_type, amount}
   ├── Response: {order_id, amount, currency}
   ↓
7. Razorpay script loads with payment options
   ↓
8. Razorpay Checkout Opens
   ├── User enters card details
   ├── Razorpay processes payment
   ├── Success/Failure response
   ↓
9. Frontend calls POST /api/payment/verify
   ├── Body: {order_id, payment_id, signature}
   ├── Backend verifies signature
   ├── If valid:
   │   ├── Update payments.status = 'completed'
   │   ├── Insert into user_courses
   │   ├── Delete from wishlist
   │   └── Return {success: true}
   ↓
10. Frontend Redirects to /success page
   ↓
11. Success page shows confirmation
   ├── Next steps guidance
   ├── Contact information
   ├── Auto-redirect after 5 seconds
   ↓
12. User redirected to Dashboard
   ↓
13. Course now appears in Courses Tab
```

---

## 🔄 Session Management

```
User Signs In
      │
      ▼
Server Sets HTTP-Only Cookie
      │
      ├─ auth_token (encrypted)
      │  └─ httpOnly, secure, sameSite=lax
      │  └─ maxAge = 7 days
      │
      ├─ user_id (readable)
      │  └─ clientSide tracking
      │  └─ maxAge = 7 days
      │
      ▼
Frontend Stores in Cookies
      │
      ▼
On Each Request
      ├─ Browser auto-includes cookies
      ├─ Backend reads auth_token
      ├─ Verifies session
      └─ Grant/Deny access
      │
      ▼
On Sign Out
      ├─ Server clears auth_token
      ├─ Server clears user_id
      ├─ Client redirects to home
      └─ Auth checks fail
```

---

## 📈 Scalability Considerations

### Database Indexing
```
✓ users (email, username)
✓ user_courses (user_id, status)
✓ wishlist (user_id, course_type)
✓ payments (user_id, status, razorpay_id)
✓ certificates (user_id, course_type)
```

### Performance Optimizations
```
• User data caching in context
• Cookie-based sessions
• Indexed database queries
• Lazy loading of components
• Image optimization
• Code splitting
```

### Future Scaling
```
• Add Redis for session caching
• Implement database read replicas
• CDN for static assets
• API rate limiting
• Load balancing
```

---

## 🔒 Security Layers

```
┌─────────────────────────────────┐
│ HTTPS/TLS (Transport)           │
├─────────────────────────────────┤
│ Input Validation (All Forms)    │
├─────────────────────────────────┤
│ PBKDF2 Password Hashing         │
├─────────────────────────────────┤
│ HTTP-Only Cookies               │
├─────────────────────────────────┤
│ Session Validation              │
├─────────────────────────────────┤
│ Razorpay Signature Verification │
├─────────────────────────────────┤
│ Database Constraints             │
├─────────────────────────────────┤
│ API Authorization Checks        │
└─────────────────────────────────┘
```

---

**Architecture Documentation**
Last Updated: March 3, 2026
Status: ✅ Complete
